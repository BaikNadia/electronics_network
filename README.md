# 📋 Electronics Network Management System
Система управления сетью по продаже электроники с иерархической структурой, 
REST API и админ-панелью.

## 🚀 Возможности
Иерархическая модель сети из трех уровней: Завод → Розничная сеть → 
Индивидуальный предприниматель
Автоматическое определение уровня на основе поставщика
Полнофункциональная админ-панель Django с кастомными фильтрами и действиями
REST API с CRUD операциями, фильтрацией и ограничениями доступа
Валидация данных на всех уровнях (модель, сериализатор, формы)
Контроль доступа только для активных сотрудников

## 🛠 Технологический стек
Backend: Django 3.2.19, Django REST Framework 3.13.1
Database: PostgreSQL
Authentication: Basic Auth, Session Auth
Tools: Django Filter, python-decouple

## 📁 Структура проекта
electronics_network/
├── electronics_network/          # Основной проект Django
│   ├── settings.py              # Настройки проекта
│   ├── urls.py                  # Корневые URL-маршруты
│   └── wsgi.py
├── network/                     # Приложение сети
│   ├── models.py               # Модели данных
│   ├── serializers.py          # Сериализаторы DRF
│   ├── views.py                # ViewSets и API представления
│   ├── admin.py               # Конфигурация админ-панели
│   ├── permissions.py         # Кастомные права доступа
│   ├── filters.py             # Фильтры для API
│   ├── urls.py                # Маршруты API
│   └── apps.py
├── manage.py
└── requirements.txt            # Зависимости проекта

## 🗄 Модели данных

### NetworkNode (Звено сети)
Основная модель, представляющая звено в иерархической сети:

Поле	       Тип	                 Описание	                           Обязательное
name	      CharField             Название звена	                            Да
email	      EmailField	        Электронная почта	                        Да
country	      CharField	            Страна                     	                Да
city	      CharField	            Город	                                    Да
street	      CharField	            Улица	                                    Да
house_number  CharField	            Номер дома	                                Да
supplier	  ForeignKey	        Поставщик (ссылка на другого NetworkNode)	Нет
node_type	  IntegerField	        Тип звена (0-Завод, 1-Розница, 2-ИП)	    Авто
products	  ManyToManyField	    Продукты	                                Нет
debt	      DecimalField	        Задолженность перед поставщиком	            Нет
created_at	  DateTimeField	        Время создания	                            Авто
is_active	  BooleanField	        Активность звена	                        Нет

### Product (Продукт)

Модель продукта, который может продаваться в сети:

Поле	        Тип	         Описание
name	       CharField	Название продукта
model	       CharField	Модель продукта
release_date   DateField	Дата выхода на рынок

### Employee (Сотрудник)
Модель для контроля доступа к API:

Поле	     Тип	           Описание
user	    OneToOneField	 Связанный пользователь Django
is_active	BooleanField	 Статус активности сотрудника
created_at	DateTimeField	 Дата создания записи

## 🏗 Иерархическая структура
Система автоматически определяет уровень звена на основе поставщика:

Уровень	   Тип	                             Условие
0	      Завод	                           supplier = null
1	      Розничная сеть	               Поставщик является заводом (supplier.node_type = 0)
2	      Индивидуальный предприниматель   Поставщик не является заводом

Пример цепочки:
ЭлектронЗавод (уровень 0, supplier: null)
    ↓
ТехноМаркет (уровень 1, supplier: ЭлектронЗавод)
    ↓
ИП Иванов (уровень 2, supplier: ТехноМаркет)

## 🚀 Быстрый старт

1. Клонирование репозитория
git clone <repository-url>
cd electronics_network

2. Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
 или
venv\Scripts\activate     # Windows

3. Установка зависимостей
pip install -r requirements.txt

4. Создание файла .env
echo "SECRET_KEY=your-secret-key-here" > .env
echo "DEBUG=True" >> .env
echo "DB_NAME=electronics_network_db" >> .env
echo "DB_USER=your_username" >> .env
echo "DB_PASSWORD=your_password" >> .env
echo "DB_HOST=localhost" >> .env
echo "DB_PORT=5432" >> .env

5. Создание и применение миграций
python manage.py makemigrations
python manage.py migrate

6. Создание суперпользователя
python manage.py createsuperuser

7. Запуск сервера
python manage.py runserver

## 📊 Админ-панель
Доступ: http://localhost:8000/admin/

Особенности админ-панели:
1. Ссылка на поставщика
В списке объектов имя поставщика является кликабельной ссылкой
При клике открывается страница редактирования поставщика

2. Фильтры
По городу (кастомный фильтр)
По стране
По типу звена
По поставщику
По активности

3. Admin Actions
Очистить задолженность перед поставщиком - обнуляет поле debt у выбранных объектов

4. Автоматические поля
node_type - определяется автоматически, не редактируется
created_at - заполняется автоматически при создании

## 🔗 REST API
Базовый URL: http://localhost:8000/api/

Аутентификация
Basic Auth: Authorization: Basic <base64(username:password)>

Session Auth: Для доступа через браузер

Endpoints
Network Nodes:
GET    /api/network-nodes/           # Получить список
POST   /api/network-nodes/           # Создать новое звено
GET    /api/network-nodes/{id}/      # Получить детали
PUT    /api/network-nodes/{id}/      # Полное обновление
PATCH  /api/network-nodes/{id}/      # Частичное обновление
DELETE /api/network-nodes/{id}/      # Удалить

Products:
GET    /api/products/                # Список продуктов
POST   /api/products/                # Создать продукт
GET    /api/products/{id}/           # Детали продукта
PUT    /api/products/{id}/           # Обновить продукт
PATCH  /api/products/{id}/           # Частично обновить
DELETE /api/products/{id}/           # Удалить продукт

Employees:
GET    /api/employees/               # Список сотрудников
POST   /api/employees/               # Создать сотрудника
GET    /api/employees/{id}/          # Детали сотрудника
PUT    /api/employees/{id}/          # Обновить
PATCH  /api/employees/{id}/          # Частично обновить
DELETE /api/employees/{id}/          # Удалить

Фильтрация и поиск
Параметры запроса для /api/network-nodes/:
Параметр	      Пример	      Описание
country	     ?country=Россия	 Фильтр по стране
city	     ?city=Москва	     Фильтр по городу
node_type	 ?node_type=0	     Фильтр по типу звена
is_active	 ?is_active=true	 Фильтр по активности
search	     ?search=Электрон	 Поиск по названию, email, городу, стране
ordering	 ?ordering=name	     Сортировка по полю
min_debt	 ?min_debt=1000	     Задолженность от
max_debt	 ?max_debt=50000	 Задолженность до

## 🔒 Безопасность и валидация
Ограничения API:
Запрет обновления debt через API

Валидация отрицательных значений:
debt не может быть отрицательным
При попытке установить debt = -100 → ValidationError

Проверка циклических зависимостей:
Объект не может быть своим собственным поставщиком
Запрещены циклические цепочки поставок

Контроль доступа:
Доступ к API только для активных сотрудников
Неактивные сотрудники получают 403 Forbidden

Права доступа:
Роль	                           API доступ	     Админ-панель
Суперпользователь	               ✅ Полный	     ✅ Полный
Активный сотрудник	               ✅ Полный	     ❌ Нет
Неактивный сотрудник	           ❌ Нет	         ❌ Нет
Пользователь без профиля Employee  ❌ Нет	         ❌ Нет

## 📈 Расширение функциональности
Планируемые улучшения:
Экспорт данных в Excel/CSV 
Графическое представление иерархии сети
Уведомления о критическом уровне задолженности
История изменений для полей модели
API документация с использованием Swagger/Redoc

